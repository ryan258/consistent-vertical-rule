consistent-vertical-rule
========================

Instead of using an image to get a top to bottom vertical rule between columns where one isn't always taller than the other you can use this.  Currently for fixed width only.
